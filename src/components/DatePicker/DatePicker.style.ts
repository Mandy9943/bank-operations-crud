import styled from "styled-components";

export const DatePickerS = styled.input`
  padding: 10px;
  border-radius: 2px;
  border: 2px solid gray;
  background: transparent;

  :focus {
    border-bottom: 2px solid darkslategray;
    outline: none;
  }
`;
